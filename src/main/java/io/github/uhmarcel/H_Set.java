
package io.github.uhmarcel;

public interface H_Set<E> extends H_Collection<E> {

    // Inherits all methods from H_Collection,
    // with the additional constraints that
    // duplicate elements are not allowed.
    // The additional interface, just as in the original 
    // implementation, is made to show the distinction 
    // on this constraint to other classes implementing
    // the interface.
    
}
